## v0.2.9

- **OfficeCLI 集成**：新增 @officecli/officecli 支持，打包流程自动恢复原生 binary；运行时自动注入 officecli MCP server，配置变更后自动重启 legalwork 进程。
- **Agent 附件本地路径引用**：附件支持 `localFilePath`，Agent 可直接引用本地文件路径，文件类任务处理更精准。
- **数据合规本地引擎优化**：Python 依赖按核心/可选 OCR 包拆分，安装完成后写入标记避免重复校验，启动速度提升；任务中断后重新打开时自动标记状态。
- **数据合规面板体验改进**：进度弹窗在任务完成/失败时也可正常关闭，状态展示更直观。
- **脱敏引擎主体识别增强**：新增法定代表人、委托诉讼代理人、律所、公司/机构等更多识别规则，脱敏覆盖更完整。
- **知识库 PDF 预览继续优化**：进一步提升 PDF 兼容性与显示稳定性。
