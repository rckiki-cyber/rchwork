# yuandian-law-search 根目录检索副本兜底忽略
# 每次检索的主归档落在 archive/（已被仓库根 .gitignore 忽略，不入库）。
# CWD 工作副本若意外落在 skill 根目录（调用方站在 skill 自身目录下时），
# 同样不应入库。代码层已在 _archive_write_report 拦截此场景，本规则作兜底。
# 文件名模式：<8位日期>_<6位时间>_<查询>.md
/[0-9][0-9][0-9][0-9][0-9][0-9][0-9][0-9]_[0-9][0-9][0-9][0-9][0-9][0-9]_*.md
